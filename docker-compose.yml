services:
  aurapilot:
    build: .
    container_name: aurapilot
    volumes:
      - ./:/app
      - logs_data:/app/logs
    stdin_open: true
    tty: true
    environment:
    - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8000:8000"

volumes:
  logs_data: